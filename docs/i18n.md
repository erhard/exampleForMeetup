## i18n

Warum i18n ?

i18n ist ein Beispiel um das Bootkonzept von Quasar zu erklären, das etwas von dem von VUE abweicht.

Der des Verzeichnisses i18n  ist wiederum in Unterverzeichnisse unterteilt. im Verzeichniss i18n ist eine index.js.  Ist eine index.js Datei vorhanden so kann man den Verzeichnisnamen selbst in einem Importtatemant verwenden.  in den index.js Dateien werden oft andere Unterverzeichnisse mit Index.js Dateien aufgerufen und diese so nach oben aggregiert.
Dies ist eine Eigenschaft von Webpack (um genauer zu sein der loader innherhalb von webpack).

Die oberste index.js. wird dann im bootmechanismus im Directory "boot" bei i18n. den messages zugewiesen und der app zur Verfügung gestellt.

Hier spielt auch die Datei quasar.conf.js eine grosse Rolle.  Im Abschnitt "boot"  steht drin, was quasar beim booten für Module ausführen soll. Das bootKonzept unterscheidet sich von vue vue hat hierzu die main.js, die man bei Quasar vergeblich sucht.



